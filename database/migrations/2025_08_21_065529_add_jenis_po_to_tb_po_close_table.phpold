<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

class AddJenisPoToTbPoCloseTable extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::table('tb_po_close', function (Blueprint $table) {
            $table->string('nomor_po', 100)
                ->nullable()
                ->after('id_po');
            $table->string('jenis_po', 100)
                ->nullable()
                ->default('operasional')
                ->after('tanggal_tutup_po');
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::table('tb_po_close', function (Blueprint $table) {
            $table->dropColumn('jenis_po');
            $table->dropColumn('nomor_po');
        });
    }
}
